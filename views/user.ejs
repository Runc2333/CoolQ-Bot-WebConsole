<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1,user-scalable=0">
    <meta name="author" content="Runc" />
    <title><%= title %></title>
    <link href="/css/materialize.min.css" rel="stylesheet" type="text/css" media="screen">
    <link href="/css/project.css" rel="stylesheet" type="text/css" media="screen">
    <link href="/css/user/user.css" rel="stylesheet" type="text/css" media="screen">
</head>

<body>
    <div class="top-panel">
        <div class="panel-icon"><i class="material-icons">engineering</i></div>
        <div class="panel-title">控制面板</div>
    </div>
    <!-- <div class="card-content" style="grid-template-rows: 100%; padding: 0 15px;place-items: center center;">
        <div class="progressContainer" style="width: 100%;">
            <div class="progressTip">正在联系老人机</div>
            <div class="progress">
                <div class="indeterminate indigo darken-4"></div>
            </div>
        </div>
    </div> -->
    <div class="card-content">
        <div class="card-title">选择一个群组</div>
        <% if(group.length > 0){ %>
        <div class="row" style="margin: 0;padding: 0 15px;overflow: auto;">
            <% group.forEach((item) => { %>
                <a class="selection waves-effect" href="/user/configureGroup?groupId=<%= item.groupId %>"><i class="material-icons">group</i>
                    <font>
                        <% if (item.role === "owner") { %>
                        <div
                            style="display:inline-block;border-radius: 3px;background-color: rgb(236,193,73);color: white;padding: 0px 3px;margin: 0 5px 0 0;">
                            群主</div>
                        <% } else if(item.role === "admin") { %>
                        <div
                            style="display:inline-block;border-radius: 3px;background-color: rgb(136,214,167);color: white;padding: 0px 3px;margin: 0 5px 0 0;">
                            管理员</div>
                        <% } else { %>
                        <div
                            style="display:inline-block;border-radius: 3px;background-color: rgb(151,187,244);color: white;padding: 0px 3px;margin: 0 5px 0 0;">
                            成员</div>
                        <% } %>
                        <%= item.groupName %>(<%= item.groupId %>)
                    </font>
                </a>
            <% }); %>
            <a class="selection waves-effect" href="/"><i class="material-icons">arrow_back</i>
                <font>返回</font>
            </a>
        </div>
        <% } else { %>
            <div class="tips">
                <i class="material-icons">sentiment_very_dissatisfied</i> 您没有加入任何群组
                <div>若您刚刚加入了一个拥有老人机的群组，则需要等到下次缓存刷新时才会显示在这里<br>此过程最多可能需要 90分钟</div>
            </div>
        <% } %>
    </div>
    <script type="text/javascript" src="/js/materialize.min.js"></script>
    <script type="text/javascript" src="/js/jquery.min.js"></script>
    <script type="text/javascript" src="/js/user/user.js"></script>
</body>
</html>